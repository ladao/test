 #markdown
  markdown readme >a.html
  firefox a.html
  vim readme

##  ls -a
# vim ./gitconfig
#change ~/.gitconfig
 
# git config --global user.name "qdj"  
#diff
##patch
  vim a
  vim b
  diff a b -u >ab.diff
  ls
  cat ab.diff 
  patch a <ab.diff 
  cat a
  cat b
  patch a <ab.diff 
  cat a
  cat b

   
  # linus git
  
    git clone git://github.com/happypeter/taiyuan-note.git
    git pull
  
    
    git init
    ls -a
    git add readme
    git commit -a -m "hello qdj"

#sudo 
 sudo apt-get install git-core

 sudo apt-get install tig

 git clone git://github.com/happypeter/taiyuan-note.git

 sudo apt-get install git-core

 sudo apt-get install tig

 ping www.nuc.eud.cn

#git clone
  git clone git://github.com/happypeter/taiyuan-note.git

  gedit note

  cat taiyuan-note

  gedit note.md

  file taiyuan-note

  cd taiyuan-note/

  gedit note.md

  sudo apt-get install vim

  vim note

  gedit note

  ls

  gedit taiyuan-note

  cat taiyuan-note

  cd taiyuan-note

  note.md

  gedit note.md

  cd ..

  gedit note.md

  sudo apt-get install vim

  vim note

#go away
       sudo dpkg --purge sox
      
       dpkg -l|grep l
       dpkg -l|grep git
       dpkg -l|grep tig
       dpkg -l|grep tigk
       dpkg -l|grep vim
      
       totem git-config.wav 

