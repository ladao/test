git clone git://github.com/happypeter/peter-vim.git
sudo apt-get install exuberant-ctags
mv peter-vim/ .vim
vim vimrc   (runtime vimrc)

http://www.beijinglug.org/

#recordmydesktop

#git checkout
##git branch
##git checkout master
##git reset --hard HEAD^
  git checkout
 
  tig
 
 git checkout 961a9312a8d1d4d61290faf12ee6fd43ce268de7

  tig

  vim aa

  ls

  git branch

  git checkout master


 scp -r peter@192.168.0.19:~/share . 
 
 Global setup:

 Download and install Git
  git config --global user.name "Your Name"
  git config --global user.email qdj123321@126.com
  Add your public key
        

Next steps:

  mkdir linus
  cd linus
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:ladao/linus.git
  git push origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:ladao/linus.git
  git push origin master
      

Importing a Subversion Repo?

  Click here
      

When you're done:

  Continue


 
  cd 
  cd .ssh/
  ls
  cat id_dsa.pub |xclip -sel clip
  
  
   sudo apt-get install xclip
   vim readme
   ssh git@github.com
   ssh  -v git@github.com
 

  rm -rf .ssh/
  ssh-keygen -t dsa
  sudo apt-get install xclip
  cat /home/akaedu/.ssh/id_dsa.pub| xclip -sel clip
  ssh git@github.com
  ssh  -v git@github.com

  git config --global user.email qdj123321@126.com
  git remote add origin git@github.com:ladao/linus.git
  git push origin master


 #markdown
  markdown readme >a.html
  firefox a.html
  vim readme

##  ls -a
# vim ./gitconfig
#change ~/.gitconfig
 
# git config --global user.name "qdj"  
#diff
##patch
  vim a
  vim b
  diff a b -u >ab.diff
  ls
  cat ab.diff 
  patch a <ab.diff 
  cat a
  cat b
  patch a <ab.diff 
  cat a
  cat b

   
  # linus git
  
    git clone git://github.com/happypeter/taiyuan-note.git
    git pull
  
    
    git init
    ls -a
    git add readme
    git commit -a -m "hello qdj"

#sudo 
 sudo apt-get install git-core

 sudo apt-get install tig

 git clone git://github.com/happypeter/taiyuan-note.git

 sudo apt-get install git-core

 sudo apt-get install tig

 ping www.nuc.eud.cn

#git clone
  git clone git://github.com/happypeter/taiyuan-note.git

  gedit note

  cat taiyuan-note

  gedit note.md

  file taiyuan-note

  cd taiyuan-note/

  gedit note.md

  sudo apt-get install vim

  vim note

  gedit note

  ls

  gedit taiyuan-note

  cat taiyuan-note

  cd taiyuan-note

  note.md

  gedit note.md

  cd ..

  gedit note.md

  sudo apt-get install vim

  vim note

#go away
       sudo dpkg --purge sox
      
       dpkg -l|grep l
       dpkg -l|grep git
       dpkg -l|grep tig
       dpkg -l|grep tigk
       dpkg -l|grep vim
      
       totem git-config.wav 

